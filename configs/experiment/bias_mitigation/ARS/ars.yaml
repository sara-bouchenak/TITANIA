# @package _global_
defaults:
  - /experiment/bias_mitigation/default.yaml
  - /data/dataset/ars.yaml
data:
  seed: 59
method:

  hyperparameters:

    model: src.FL_core.utils.net.LogRegression

    client:

      loss: BCELoss

      optimizer:
        name: SGD
        lr: 0.001
        weight_decay: 0.01

      batch_size: 1024
    
    server:
      time_to_accuracy_target: 0.8
      workers: -1