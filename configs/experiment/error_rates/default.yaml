# @package _global_
defaults:
  - /data/noise_injection: default.yaml
  - override /data/others: union_clients_test.yaml
  - override /save: null
  - override /eval: only_global_model.yaml
  - override /method: default.yaml
  - override /hydra: error_rates.yaml

protocol:
  n_clients: 10
  n_rounds: 150
