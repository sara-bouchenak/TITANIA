# @package _global_
defaults:
  - /experiment/overall_impact/Heart/heart.yaml

method:

  hyperparameters:

    model: src.FL_core.utils.net.SVM

    client:

      loss: torchmetrics.classification.BinaryHingeLoss

      optimizer:
        name: RMSprop
        lr: 0.03
        weight_decay: 0
