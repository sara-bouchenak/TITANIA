# @package _global_
defaults:
  - /experiment/overall_impact/default.yaml
  - override /data/dataset: meps.yaml
  - override /data/others: union_clients_test_wo_val.yaml

data:
  seed: 38

method:

  hyperparameters:

    server:
      time_to_accuracy_target: 0.85

    client:
      batch_size: 128
