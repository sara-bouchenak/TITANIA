# @package _global_
defaults:
  - /experiment/overall_impact/Adult_001/adult.yaml

method:

  hyperparameters:

    model: src.FL_core.utils.net.MLP

    net_args:
      n_hidden_layer_1: 64
      n_hidden_layer_2: 32

    client:

      loss: BCEWithLogitsLoss

      optimizer:
        name: RMSprop
        lr: 0.0001
        weight_decay: 0.01
