# @package _global_
defaults:
  - /experiment/overall_impact/default.yaml
  - override /data/dataset: heart.yaml
  - override /data/others: union_clients_test_wo_val.yaml

data:
  seed: 200

method:

  hyperparameters:

    server:
      time_to_accuracy_target: 0.7

    client:
      batch_size: 128
