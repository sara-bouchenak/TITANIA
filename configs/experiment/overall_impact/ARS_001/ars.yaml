# @package _global_
defaults:
  - /experiment/overall_impact/default.yaml
  - override /data/dataset: ars.yaml

data:
  seed: 59

method:

  hyperparameters:

    server:
      time_to_accuracy_target: 0.95

    client:
      batch_size: 1024
