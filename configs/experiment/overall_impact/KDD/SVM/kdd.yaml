# @package _global_
defaults:
  - /experiment/overall_impact/KDD/kdd.yaml

method:

  hyperparameters:

    model: src.FL_core.utils.net.SVM

    client:

      loss: torchmetrics.classification.BinaryHingeLoss

      optimizer:
        name: SGD
        lr: 0.01
        weight_decay: 0
